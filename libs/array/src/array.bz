Array $Module










len :: []T' ;; I64
len :: $arrLength

->  :: ([]T'. I64) ;; (T' Maybe)
->  :: $getIndex

=>  :: ([]T'. T'. I64) ;; []T'
=>  :: $setIndex










(T') Slice :: (
    arr    : []T'.
    start  : I64.
    end    : I64.
    stride : I64.
    group  : I64 )










-> :: (T Slice. I64) ;; (T Maybe)
(s. i) -> :: "Work Out Later"

=> :: (T Slice. T. I64) ;; (T Slice)
(s. x. i) => :: "Work Out Later"
